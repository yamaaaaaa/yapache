{
  "name": "yapache",
  "version": "1.0.0",
  "description": "small web server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . yapache --platform=darwin --arch=x64 --overwrite"
  },
  "author": "Yamaaaaa",
  "license": "MIT",
  "devDependencies": {
    "electron": "^4.1.0"
  },
  "dependencies": {
    "electron-packager": "^13.1.1",
    "express": "^4.16.4",
    "ipc": "0.0.1",
    "os": "^0.1.1"
  }
}
